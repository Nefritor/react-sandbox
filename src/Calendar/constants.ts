import { Day, DayMs, FullWeek, HourMs, MinuteMs, SecondMs, WeekDaysCount, WeekMs } from './_constants/Day';

export {
    Day,

    FullWeek,
    WeekMs,
    DayMs,
    HourMs,
    MinuteMs,
    SecondMs,
    WeekDaysCount
};
